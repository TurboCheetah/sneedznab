version: '3.7'

services:
  sneedznab:
    image: ghcr.io/turbocheetah/sneedznab:master
    container_name: sneedznab
    env_file:
      - .env
    ports:
      - '3000:3000'
    restart: 'unless-stopped'
